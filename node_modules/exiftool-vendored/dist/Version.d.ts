import { ExifDate } from "exiftool-vendored/dist/ExifDate";
import { ExifDateTime } from "exiftool-vendored/dist/ExifDateTime";
import { ResourceEvent } from "exiftool-vendored/dist/ResourceEvent";
/**
 * @see https://exiftool.org/TagNames/XMP.html#Version
 */
export interface Version {
    Comments?: string;
    Event?: ResourceEvent;
    Modifier?: string;
    ModifyDate?: ExifDateTime | ExifDate | string;
    Version?: string;
}
export declare function isVersion(obj: unknown): obj is Version;
