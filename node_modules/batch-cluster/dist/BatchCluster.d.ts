import { BatchClusterEmitter, ChildEndReason } from "batch-cluster/dist/BatchClusterEmitter";
import type { BatchClusterOptions } from "batch-cluster/dist/BatchClusterOptions";
import type { BatchClusterStats } from "batch-cluster/dist/BatchClusterStats";
import type { BatchProcessOptions } from "batch-cluster/dist/BatchProcessOptions";
import type { ChildProcessFactory } from "batch-cluster/dist/ChildProcessFactory";
import type { CombinedBatchProcessOptions } from "batch-cluster/dist/CombinedBatchProcessOptions";
import { Deferred } from "batch-cluster/dist/Deferred";
import { Task } from "batch-cluster/dist/Task";
export { BatchClusterOptions } from "batch-cluster/dist/BatchClusterOptions";
export { BatchProcess } from "batch-cluster/dist/BatchProcess";
export { Deferred } from "batch-cluster/dist/Deferred";
export * from "batch-cluster/dist/Logger";
export { SimpleParser } from "batch-cluster/dist/Parser";
export { kill, pidExists } from "batch-cluster/dist/Pids";
export { Task } from "batch-cluster/dist/Task";
export type { Args } from "batch-cluster/dist/Args";
export type { BatchClusterEmitter, BatchClusterEvents, ChildEndReason, TypedEventEmitter, } from "batch-cluster/dist/BatchClusterEmitter";
export type { WithObserver } from "batch-cluster/dist/BatchClusterOptions";
export type { BatchClusterStats } from "batch-cluster/dist/BatchClusterStats";
export type { BatchProcessOptions } from "batch-cluster/dist/BatchProcessOptions";
export type { ChildProcessFactory } from "batch-cluster/dist/ChildProcessFactory";
export type { CombinedBatchProcessOptions } from "batch-cluster/dist/CombinedBatchProcessOptions";
export type { HealthCheckStrategy } from "batch-cluster/dist/HealthCheckStrategy";
export type { InternalBatchProcessOptions } from "batch-cluster/dist/InternalBatchProcessOptions";
export type { LoggerFunction } from "batch-cluster/dist/Logger";
export type { Parser } from "batch-cluster/dist/Parser";
export type { HealthCheckable, ProcessHealthMonitor, } from "batch-cluster/dist/ProcessHealthMonitor";
export type { TaskOptions } from "batch-cluster/dist/Task";
export type { WhyNotHealthy, WhyNotReady } from "batch-cluster/dist/WhyNotHealthy";
/**
 * BatchCluster instances manage 0 or more homogeneous child processes, and
 * provide the main interface for enqueuing `Task`s via `enqueueTask`.
 *
 * Given the large number of configuration options, the constructor
 * receives a single options hash. The most important of these are the
 * `ChildProcessFactory`, which specifies the factory that creates
 * ChildProcess instances, and `BatchProcessOptions`, which specifies how
 * child tasks can be verified and shut down.
 */
export declare class BatchCluster {
    #private;
    readonly options: CombinedBatchProcessOptions;
    readonly emitter: BatchClusterEmitter;
    constructor(opts: Partial<BatchClusterOptions> & BatchProcessOptions & ChildProcessFactory);
    /**
     * @see BatchClusterEvents
     */
    readonly on: <E extends keyof import("batch-cluster/dist/BatchClusterEmitter").BatchClusterEvents>(eventName: E, listener: (...args: import("batch-cluster/dist/Args").Args<import("batch-cluster/dist/BatchClusterEmitter").BatchClusterEvents[E]>) => void) => BatchClusterEmitter;
    /**
     * @see BatchClusterEvents
     * @since v9.0.0
     */
    readonly off: <E extends keyof import("batch-cluster/dist/BatchClusterEmitter").BatchClusterEvents>(eventName: E, listener: (...args: import("batch-cluster/dist/Args").Args<import("batch-cluster/dist/BatchClusterEmitter").BatchClusterEvents[E]>) => void) => BatchClusterEmitter;
    get ended(): boolean;
    /**
     * Shut down this instance, and all child processes.
     * @param gracefully should an attempt be made to finish in-flight tasks, or
     * should we force-kill child PIDs.
     */
    end(gracefully?: boolean): Deferred<void>;
    /**
     * Submits `task` for processing by a `BatchProcess` instance
     *
     * @return a Promise that is resolved or rejected once the task has been
     * attempted on an idle BatchProcess
     */
    enqueueTask<T>(task: Task<T>): Promise<T>;
    /**
     * @return true if all previously-enqueued tasks have settled
     */
    get isIdle(): boolean;
    /**
     * @return the number of pending tasks
     */
    get pendingTaskCount(): number;
    /**
     * @returns {number} the mean number of tasks completed by child processes
     */
    get meanTasksPerProc(): number;
    /**
     * @return the total number of child processes created by this instance
     */
    get spawnedProcCount(): number;
    /**
     * @return the current number of spawned child processes. Some (or all) may be idle.
     */
    get procCount(): number;
    /**
     * @return the current number of child processes currently servicing tasks
     */
    get busyProcCount(): number;
    get startingProcCount(): number;
    /**
     * @return the current pending Tasks (mostly for testing)
     */
    get pendingTasks(): readonly Task[];
    /**
     * @return the current running Tasks (mostly for testing)
     */
    get currentTasks(): Task[];
    /**
     * For integration tests:
     */
    get internalErrorCount(): number;
    /**
     * Verify that each BatchProcess PID is actually alive.
     *
     * @return the spawned PIDs that are still in the process table.
     */
    pids(): number[];
    /**
     * For diagnostics. Contents may change.
     */
    stats(): BatchClusterStats;
    /**
     * Get ended process counts (used for tests)
     */
    countEndedChildProcs(why: ChildEndReason): number;
    get childEndCounts(): Record<NonNullable<ChildEndReason>, number>;
    /**
     * Shut down any currently-running child processes. New child processes will
     * be started automatically to handle new tasks.
     */
    closeChildProcesses(gracefully?: boolean): Promise<void>;
    /**
     * Reset the maximum number of active child processes to `maxProcs`. Note that
     * this is handled gracefully: child processes are only reduced as tasks are
     * completed.
     */
    setMaxProcs(maxProcs: number): void;
    /**
     * Run maintenance on currently spawned child processes. This method is
     * normally invoked automatically as tasks are enqueued and processed.
     *
     * Only public for tests.
     */
    vacuumProcs(): Promise<void[]>;
}
